% Fig 5:fig6b — Arquitectura hexagonal del scraper-tiktok
\begin{tikzpicture}[
    node distance=0.6cm,
    every node/.style={font=\footnotesize},
    layer/.style={draw, rounded corners=2pt, minimum width=10cm, minimum height=1.4cm, align=center, fill=blue!5},
    domain/.style={layer, fill=green!8, minimum height=1.6cm},
    app/.style={layer, fill=yellow!10},
    adapt/.style={layer, fill=orange!10, minimum height=1.6cm},
    driver/.style={layer, fill=red!8, minimum width=4cm, minimum height=0.9cm},
    arrow/.style={-Stealth, thick, gray!70}
]
    \node[domain] (dom) {%
        \textbf{domain/} \\[2pt]
        \texttt{entities.py} (Profile, Video, StageRecord, RunAudit) \quad\textbar\quad \texttt{ports.py} (ProfileScraperPort, VideoScraperPort, StoragePort) \quad\textbar\quad \texttt{utils.py}
    };
    \node[app, below=of dom] (app) {%
        \textbf{application/} \\[2pt]
        \texttt{ScrapeService} — wires ports, tracks per-stage timings, writes \texttt{run\_audit.json}
    };
    \node[adapt, below=of app] (adapt) {%
        \textbf{adapters/} \\[2pt]
        \texttt{profile\_playwright.py} (Playwright/Chromium) \quad\textbar\quad \texttt{video\_ytdlp.py} (yt-dlp + cookies) \\
        \texttt{storage\_filesystem.py} $\rightarrow$ \texttt{data/<run\_id>/}
    };
    \node[driver, below=of adapt] (main) {\textbf{main.py} (CLI / driving adapter)};

    \draw[arrow] (main) -- (adapt) node[midway, right, font=\scriptsize] {invoca};
    \draw[arrow] (adapt) -- (app) node[midway, right, font=\scriptsize] {implementa};
    \draw[arrow] (app) -- (dom) node[midway, right, font=\scriptsize] {usa contratos};

    \node[right=0.3cm of dom, font=\scriptsize\itshape, text width=2.5cm, align=left] {sin deps externas};
    \node[right=0.3cm of adapt, font=\scriptsize\itshape, text width=2.5cm, align=left] {Playwright, yt-dlp, FS};
\end{tikzpicture}

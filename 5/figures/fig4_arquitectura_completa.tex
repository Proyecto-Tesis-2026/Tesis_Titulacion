% Fig 6:fig4 -- Arquitectura completa del sistema PeruVox.
\begin{tikzpicture}[
    node distance=0.4cm,
    every node/.style={font=\scriptsize, align=center, inner sep=4pt},
    src/.style={draw, rounded corners=2pt, fill=blue!12, minimum width=1.8cm, minimum height=0.5cm},
    pipe/.style={draw, rounded corners=2pt, fill=yellow!15, minimum width=2.2cm, minimum height=0.55cm},
    db/.style={draw, rounded corners=2pt, fill=orange!12, minimum width=2.4cm, minimum height=0.85cm},
    mcp/.style={draw, rounded corners=2pt, fill=green!15, minimum width=2.8cm, minimum height=1cm},
    cli/.style={draw, rounded corners=2pt, fill=purple!12, minimum width=2.6cm, minimum height=0.85cm},
    arrow/.style={-{Stealth[length=2mm]}, thick}
]
    % Capa 1: 4 fuentes
    \node[src] (s1) {noticias};
    \node[src, right=0.2cm of s1] (s2) {twitter};
    \node[src, right=0.2cm of s2] (s3) {youtube};
    \node[src, right=0.2cm of s3] (s4) {tiktok};
    \node[font=\scriptsize\itshape, below=0.05cm of s2, xshift=0.7cm] {Bronze (raw JSON)};

    % Capa 2: pipeline 8 pasos
    \node[pipe, below=1.2cm of s1] (p1) {ingest};
    \node[pipe, right=0.15cm of p1] (p2) {embed};
    \node[pipe, right=0.15cm of p2] (p3) {label};
    \node[pipe, right=0.15cm of p3] (p4) {consolidate};
    \node[pipe, below=0.15cm of p1] (p5) {llm\_dedup};
    \node[pipe, right=0.15cm of p5] (p6) {load\_silver};
    \node[pipe, right=0.15cm of p6] (p7) {build\_graph};
    \node[pipe, right=0.15cm of p7] (p8) {refresh\_gold};
    \node[font=\scriptsize\itshape, above=0.05cm of p1, xshift=2cm] {Pipeline diario (electoral-intelligence)};

    % Capa 3: BDs
    \node[db, below=1.2cm of p5, xshift=0.5cm] (pg)  {PostgreSQL \\ \scriptsize + pgvector (Silver/Gold)};
    \node[db, right=0.6cm of pg] (neo) {Neo4j Community \\ \scriptsize grafo + GDS/APOC};

    % Capa 4: MCP server + cliente
    \node[mcp, below=1cm of pg, xshift=1.2cm] (mcp) {electoral\_mcp \\ \scriptsize 13 typed tools};
    \node[cli, right=1cm of mcp] (cli) {Cliente MCP \\ \scriptsize Claude Desktop / web};

    % Conexiones
    \draw[arrow] (s1.south) -- (p1.north);
    \draw[arrow] (s2.south) -- (p1.north);
    \draw[arrow] (s3.south) -- (p1.north);
    \draw[arrow] (s4.south) -- (p1.north);
    \draw[arrow, dashed, gray] (p6.south) -- ++(0,-0.3) -| (pg.north);
    \draw[arrow, dashed, gray] (p7.south) -- ++(0,-0.3) -| (neo.north);
    \draw[arrow] (pg.south)  -- (mcp.north);
    \draw[arrow] (neo.south) -- (mcp.north);
    \draw[arrow] (mcp) -- node[above, font=\scriptsize\itshape] {MCP / streamable-http} (cli);
\end{tikzpicture}
